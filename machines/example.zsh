# Example machine-specific configuration
# Copy this file and rename it to match your hostname: $(hostname -s).zsh

# Example: Git configuration override
# git config --global user.email "different@email.com"

# Example: Machine-specific PATH additions
# export PATH="/opt/local/bin:$PATH"

# Example: Machine-specific aliases
# alias work="cd ~/Work"

# Example: Environment-specific settings
# export AWS_PROFILE="personal"

# Example: Performance tuning for high-end machines
# export NODE_OPTIONS="--max-old-space-size=8192"

# Example: Display info on load (optional)
# echo "Loaded machine config: $(hostname -s)"
